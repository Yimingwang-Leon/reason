# PLAN.md — RL 突围最终作战计划(总指挥合成版)

**日期 2026-06-11 晚。输入:BRIEF + 3 recon + 5 提案 + 3 红队判决 + 本指挥部当晚实查。**
**状态:设计冻结产物,$0 已花。执行需用户解锁(见 §7)。**

---

## 0. 指挥部裁定(一页)

1. **账本三处强制修正,全计划按修正后执行:**
   - **地板 = 0.86,不是 0.85**(红队 V1 实查 kaggle 提交史:53414820 "public 0.86 adapter (kienngx) reproduce" = 0.86 COMPLETE 在账)。任何挑战者打出 0.85/0.86 对最终名次贡献 **= 0**;只有显示 0.87(真分 ≥0.865)改变结局。终选必含 0.86 地板票。
   - **世界 B 坐实(本指挥部今晚实查,A 级)**:`data/run012_holdout_decode.jsonl` 已解码完 770/770 行——cipher/gravity/numeral/unit **全部 110/110**,bit 91/110,eq 84/110,crypt 0/110。holdout 外推 ≈0.866 vs LB 0.84,缺口全在 LB 侧 → **所有 holdout 口径收益锁 0.7 折扣,默认保守档**。
   - **诚实校准**:run-012 预登记中心 0.855-0.86 → 实际 0.84(高估 1.5-2pp 前科)。本计划所有中心值已按世界 B + 前科双重下修;**中心情形(显示 0.85-0.86)= 无效奖。花钱买的不是"+0.5pp 中心",而是一张 P≈15-22% 的右尾彩票(真分 ≥0.865)+ 情报。** 这是给用户的定价口径,不做安慰。
2. **组合 = 05 修订版总纲:一条付费主攻线串行升级(RFT → G2 闸后才许 DPO 附属臂 / GRPO 条件武器),融合免费线全程并行。** 三条训练线吃同一个 ~68 题猎物池,采样费占 60-85%,并行 = 付三遍——串行是成本时钟下唯一自洽解(三红队独立同判)。
3. **猎物池(holdout 口径,待 C1 官方判分语义审计修正):bit R-loss ~32 + crypt solver-ok 26 + eq R-loss ~10 ≈ 68 题 ≈ +3.6pp 理论上限;0.7 折扣后 LB 可达 ≈ +2.5pp。** 达 0.865 需吃下折后池的 ~60% —— 高位执行 + 彩票配合才够,这就是 P 低的原因。结构性锁死 ~181 题/9.5pp(crypt 死区 139 + eq qop_unseen ~20 + bit oracle-dead 22)明示不碰。
4. **兜底结构(三层,任何分支都成立):** ① run-011 adapter/checkpoint 物理不动、永不覆盖;② 不达标不提交、训练产物全是新 ckpt;③ 终选 = **{0.86 地板票, 最佳挑战者}**(若规则审计否决他人 adapter 衍生 → 回退 {run-011 0.85, 最佳挑战者},预登记)。

---

## 1. 路线排序与组合(红队修正后形态;KILL 项不进计划)

### 1.1 排序总表

| 序 | 路线 | 判决→修正后形态 | 预算 | 启动条件 |
|---|---|---|---|---|
| 并行① | **04 融合彩票**(T-C/T-A/条件 T-D) | GO(3 条警告全采纳) | **$0**(≤$5 杂项)+ ≤6 配额 | 今晚,用当日过期配额;kienngx 验明正身是 T-A 硬前置 |
| 主攻② | **02 RFT**(拒绝采样 SFT + replay) | FIX→ 修正版主攻 | 保守档核心,~$30 | D1 晨,用户解锁后;M0 闸先行 |
| 条件③ | **03 DPO/RPO** | FIX→ **只许 02 附属臂**($6-9),独立臂已删 | 标准档 | **G2 分支判据成立**(覆盖高+翻转低)且不早于 06-13 |
| 条件④ | **01 GRPO** | FIX→ **降级为 R3 条件武器**(50 步单腿 ≤$60-100),独立 $280 双腿建制已删 | 激进档 | G2 同判据 + 激进档解锁;Phase-0 并入 M0 |
| 框架⑤ | **05 playbook** | FIX→ 修订后为总纲(本文即修订落地) | — | — |

### 1.2 各路线修正后规格(预登记,不许事后挪)

**④04 融合(免费并行线)**
- 今晚发 3 票:**T-C**(011⊕012 ΔW-TIES,烟雾测试+跨 init 合并 R 保持性探针)、**T-A1**(kienngx-0.86 宿主 + λ=0.25 注入 run-011 ΔW)、**T-A2**(λ=0.5+DARE p=0.9)。
- 硬前置闸:kienngx 包 r/alpha/键集/dtype 验明正身(r>32 → 先 SVD 截 32 并**重新预登记下修**);五重诊断闸(键审计/MoE 键 100%/范数 ±5%/能量 ρ≥0.90/NaN 扫)任一 FAIL = 废票不提交。
- 读法修正(红队 C9 + evidence-04①):T-C ≤0.79 只结论"本对失败、T-A 降剂量",**不外推"跨方言 R-崩实锤"**(run-012 含毒成分混杂);T-C 顺利**也不许** T-A 越过 λ=0.25 起步档。
- 预登记:T-A1 中心 0.86(=无效奖),P(0.87)≈10%;T-C 中心 0.84。止损:06-13 晨无任何票 ≥0.87 → 路线关闭,配额全让训练线。T-D(三方 TIES)仅当 T-A 任一 ≥0.86 才发。
- **不发**:同轨迹 SWA 族(已开奖 0.85/0.85,封盘)、alpha/范数缩放族(已开奖 −1pp,死刑)、run-005 系、按类拼接。

**②02 RFT(付费主攻,修正版)**
- 弹药:train_split 猎物 ~300-335 题(bit R-loss 100-150 + crypt solver-ok ~135 剔 `}`truth + eq R-loss;**eq 签名类默认不入池**,M0 直测 ≥25% 覆盖才许进)× K16;**crypt harvest 直接 cap 4096 采**(营救成功 trace 实测 913-1628 字符,7680 只产截断垃圾,省钱+尾延迟减半)。
- **选样硬化(安全 C5,防 luck-correct 蒸馏毒)**:数值类入选要求精确串等或 rel_tol≤1e-4;grader 1e-2 容差只用于记分,不用于选样。其余卫生:stop-only、crypt≤3000 tok、≤2 条/题、复读 n-gram>0.3 弃。
- **语料账修正**:唯一正样本中心 ~200 条,batch 配比 24/64 下实际重复 ~2.6 次 = 正样本视角 2.6ep。承认之,**步数砍到 ~12-15 步**(或维持 22 步但把正样本占比降到 ~25%),LR 1.2e-5 恒定 + grad_clip 1.0,事故积维持 ≤1/18。配比:正样本 + self-distill 锚(识别 pass 贪心对的 trace,KL≈0)+ replay,锚合计 ≥60%。
- **账目重标(红队 evidence-02)**:crypt 中心 **+1~2 题**(17% 是已训题+n=12+CI 2-48% 口径,holdout 26 题 M0 直测后再改);泛化分记 bit 40-60% / crypt 20-40%;**holdout 中心 +8~12 题 → 世界 B 折后 LB ≈ +0.4~0.55pp**。LB#1 中心 **0.850-0.853**;LB 只认 ≥0.86 为实质信号,±0.3pp 级去留全由 holdout 猎物逐题翻转判(LB 粒度 0.01 读不出)。
- 熔断(安全 C2/C3):探针 ≥150 题(四健康类各 ~30 + **eq 已对 ≥40 + bit 已对 ≥40**),触发线净回撤 ≥3%,附长度漂移/重复率预警;**step-11 中点探针**($0.1);回滚纪律 **probe→pass→save**,只有过探针的 state 是合法回滚点,常驻 2 个;eq spot 净回撤 ≥3 题 = 与锚熔断同级。
- 猎物名单先过 C1 官方判分语义复核(见 §4 今晚清单)再定闸门分母。同一 68 题 holdout 探针**不得做第 3 轮以上去留依据**。

**③03 DPO/RPO(附属臂,修正版)**
- 形态:只作 02 round-1 之后的附加损失(RPO = DPO+NLL),**100% 复用 02 rollouts**(零增量采样),边际 $6-9;对照臂 = rft_r1 本体,不另训。
- 启动硬条款:挂 05-G2 分支判据(覆盖 ≥30% 且翻转 <40%),不早于 06-13;02 的 P2/P3 证伪 → 本臂自动作废。
- crypt pair 约束(安全 C6 三选一,预登记选 **(b)**):crypt pair 占比 ≤25% 且探针含 eq 已对 ≥40(rejected=截断枚举开局,与 eq 枚举风格同构,是本臂最大 R-崩通道)。
- 归因:主指标 = holdout 预登记猎物逐题对比(±2 题可分辨);**LB 只发单臂**(holdout 赢家),省配额;长度混杂写入读法——若边际全来自 crypt 而 bit 无边际,即"短>长"长度效应实锤,不记"压错模态"功劳。"β-KL 内生锚"从反遗忘记账删除(只护 pair 分布)。保留 P2 真熔断:chosen logprob 连降 3 步 → 关 DPO 项退化 RFT。首批 ref logprob 先跑 10 对核 prefill 账单再放量。

**④01 GRPO(R3 条件武器,修正版)**
- 形态:50 步单腿,**钉死 ≤1000 题 × K8 × 2 pass**(顶配 K16×3pass = $120-140 仅采样,超档,禁止);cohort = M0/R1 已证"覆盖高、RFT 翻转不动"的残余猎物;**crypt rollout cap 4096**(0/1 reward 下与 7680 等价,白付 80% 钱);LR 2e-5、A=r−mean(组)、零方差组丢弃、importance_sampling→不稳切 cispo。
- 安全(C2/C3/C4):**KL 子采样锚 step-0 默认开**(coef 0.05/15% 子采样,~$6/腿,不接受"熔断二次触发才开");探针 ≥150 题含 eq/bit;probe→pass→save;判停只认 holdout 翻转、不认 train reward 曲线(prey 复用 ~6 遍会虚高)。
- 时钟预登记:真实 wall-clock 23-45 min/步(挖矿吞吐外推);前 5 步实测 >12 min/步 → 步数砍到 40;**首个 LB 读数晚于 06-14 18:00 → 不再发任何后续腿**。
- 解锁线:G2 分支判据 + 激进档授权 + 当前最佳挑战者 LB ≥0.86(0.85 续命无意义,F4)。
- 中心 +0.4~0.7pp、P(0.865)≈0.12-0.18(已按红队重标)。

### 1.3 不进计划(KILL/封盘汇总)
01 独立 $280 双腿建制;03 独立 $19-22 臂;05 旧 P5 彩票行(SWA×2/alpha/ep0,已全开奖,清零);eq 签名类默认入池(死区近邻,先测后入);crypt rollout cap 7680;"收口压力是 RL 独有杠杆"作为 GRPO 差异化卖点(不成立);死亡名单全部条目(crypt 死区/synth/closed-form>22.5%、eq tiebreak/qop_unseen、bit 断言格式、CoT 重设计、lm_head、滤 MoE、0.16.1);**新登记死刑**:lora_alpha/范数缩放 ≥+12.5%(A 级 −1pp)、同轨迹 epoch-SWA(两点实测平)。

---

## 2. 逐日时刻表(06-11 → 06-15;出分一律按 8h 最坏排;5 提交/天)

### D0 · 06-11 今晚($0,除提交配额;详细清单见 §4)
| 时段 | 动作 |
|---|---|
| 即刻 | /tmp 资产抢救 cp 进 repo(重启即灭);kienngx 3.27GB 下载 + r/alpha/键集/dtype 验证(T-A 硬前置) |
| 晚 | **C1 官方判分语义审计**(免费,改写 bit 32 题分母):notebook 版 fallback 重打 770 行解码 + 二进制守卫钉死 + 产出修正后猎物名单;比赛规则原创性条款自查 |
| 晚 | `rl/merge_lora.py` 写完 → T-C/T-A1/T-A2 构建 + 五闸诊断;**用户拍板后**串行上传提交(3 票 ×0.5-1.5h,上传 1.5-4.5h,尽早发车;当日配额过期作废) |
| 夜 | RFT/GRPO 全部代码写完 + **离线 mock dry-run**(回放 /tmp 已落盘 rollouts 过 datum 构造/熔断/journal 路径,联调风险前置到 $0);PATHS/BRIEF 账本更新;配额台账建立 |
| **G0 闸** | 已实质完成:世界 B 锁定 → 0.7 折扣 + 保守档默认。剩余输出:C1 修正后的猎物名单、kienngx 验证结论、规则审计结论 |

### D1 · 06-12(需用户解锁;保守档发车)
| 时段 | 动作 | 花费 |
|---|---|---|
| 08:00 | 免费前置:`list_checkpoints` 确认 `cad6ab5c…/weights/state_ep1`(缺失→state_ep0 降级或 $10 重放,预算列支);Tinker 余额 ≥1.3× 当档 | $0 |
| 09:00-12:00 | **M0 测量包**:holdout 猎物直测 K8 @T1.0(bit ~32 @7680 + crypt 26 @4096 + eq R-loss ~10 + eq 签名 8-10)+ 探针基线 200 贪心 | ~$3-5 |
| 12:00 | **G1 闸**(K8 口径,见 §2.1 闸门表) | — |
| 13:00-18:00 | G1 过 → **R1**:识别 pass 2130 贪心($6)→ harvest ~305 题 ×K16(crypt cap4096,$12-13)→ C5 硬化过滤 → 训练 12-15 步 @1.2e-5(step-11 中点探针)→ 评测(holdout 猎物 68 + 锚 200 + spot 300) | ~$23-25 |
| 19:00-22:00 | 过 P3 闸(猎物翻转 ≥6,锚 ≥98%)→ build adapter、**≤22:00 提交 rft_r1**;融合线按 T-C/T-A 昨夜读数发 ≤2 张跟进票 | ~$1 |
| 当日证伪 | P2:harvest bit <10% 且 crypt <8% 有正样本 → **RFT 全停($21 止)**,rollouts 移交;P3:holdout 探针翻转 <4 题或锚 <97% → 停训回滚($25 止) | |

### D2 · 06-13(分支日)
| 时段 | 动作 |
|---|---|
| 06:00-11:00 | 读 LB(rft_r1 22:00 提交 + 8h ≈ 06:00 后出);**G2 闸**:holdout 猎物净翻转 + 锚 200 + eq/bit spot |
| 11:00-18:00 | 分支:**≥+6 翻转** → 标准档解锁 R2a(DPO 附属臂,复用 r1 rollouts,先 10 对核 prefill 账单)或 R2b(未破题补采 K16+RFT-v2,二选一);**覆盖 ≥30% 且翻转 <40% + 激进档授权 + LB≥0.86** → R3 GRPO 14:00 前发车(50 步,06-14 晨完);**+3~+5** → 只保留 D3 consolidation;**<+3** → 付费线终局 |
| 19:00-22:00 | R2 产物评测;holdout 单臂赢家 ≤22:00 提交(1-2 张) |
| **22:00** | **全局证伪线:累计 holdout 猎物净翻转 <+5 → 一切花钱冻结,D3/D4 只跑融合与选点** |

### D3 · 06-14(收尾日)
| 时段 | 动作 |
|---|---|
| 晨 | 读 D2 晚 LB;R3 若在跑:收腿 → 评测 → 打包上传(**18:00 前拿不到首个 RL LB 读数 → 放弃一切后续腿**) |
| 上午 | **D3 consolidation**(可选,$15):赢家配方并入全 train(含 holdout 猎物补采 $2.4)重训。**预登记:D3 之后不再做任何 holdout 增益声明(仪器报废);pre-D3 最佳挑战者提交物理保留** |
| 下午-晚 | 提交:consolidation + 融合加注(若 T-A 任一 ≥0.86 → T-D)+ 最佳重交,≤4 张,≤22:00 |
| **G3 闸** | 挑战者 LB ≥0.86 → 决赛圈再加注;≤0.85 → 收摊。**只有显示 0.87 改变结局** |

### D4 · 06-15(截止日)
- **不开任何新训练**。只做选点与补交;最后一发不晚于截止前 4h。
- **终选锁 2 份:{0.86 地板票(53414820 或其衍生最佳), 最佳挑战者}**;若规则审计否决衍生 → {run-011 0.85, 最佳挑战者}。run-011 资产全程物理不动。

### 2.1 闸门总表(go/no-go + 预登记证伪线)

| 闸 | 时点 | 预登记预测(中心) | go | no-go(证伪线,触线即停不讨价) |
|---|---|---|---|---|
| **G0** | D0 夜 | 世界 B(已坐实);C1 审计后 bit 猎物 ≥24/32 存活 | 名单修正后发 M0 | bit 猎物经官方语义审计缩水 >50% → P1 块砍半,标准档以上暂缓 |
| **G1**(M0,K8 口径) | D1 午 | bit 覆盖 35%(15-55);crypt 12%(5-30);eq 签名 — | bit≥20% 或 crypt≥8% → R1 | **bit<20% → P1 砍半 + 锁保守档;bit 与 crypt 双双 <10% → 付费线全弃(损失 ≤$5),融合-only** |
| **P2/P3**(02 内) | D1 午后 | harvest 正样本题占比 bit 35%/crypt 12%;训后 holdout 猎物翻转 +6~8 | 提交 rft_r1 | P2:bit<10% 且 crypt<8% → $21 止;P3:翻转 <4 或锚 <97% → $25 止回滚 |
| **G2** | D2 上午 | holdout 猎物净翻转 +6(+2~+12);锚 200 ≥98% | ≥+6 → R2;覆盖≥30%&翻转<40% → R3 候选 | <+3 → 付费线终局;+3~5 → 只 D3;eq spot 回撤 ≥3 → 熔断同级回滚 |
| **全局** | 06-13 22:00 | 累计净翻转 ≥+5 | 继续 | **<+5 → 全线停花钱,保地板** |
| **G3** | D3 | 挑战者 LB 0.85-0.86(中心=无效奖) | ≥0.86 → 决赛圈 | ≤0.85 → 收摊选点 |
| 融合止损 | 06-13 晨 | T-A1 0.86 / T-C 0.84 | 任一 ≥0.87 → 加注 | 无 ≥0.87 → 融合关闭,配额让渡 |

### 2.2 提交配额台账(全 campaign 唯一台账,化解 03/04/05 双重认领)
| 日 | 计划用 | 内容 | 累计/25 |
|---|---|---|---|
| 06-11 | 3 | T-C、T-A1、T-A2(当日过期配额) | 3 |
| 06-12 | ≤3 | rft_r1 + 融合跟进 ≤2 | 6 |
| 06-13 | ≤3 | R2 holdout 单臂赢家 + 保险/融合 | 9 |
| 06-14 | ≤4 | consolidation + R3 产物 + T-D + 最佳重交 | 13 |
| 06-15 | ≤3 | 终选补交(留 2 缓冲) | 16 |

### 2.3 工程红线(每笔发车前核对)
余额 ≥1.3× 当档;rollout 边采边落盘(id/tokens/logprobs/reward/stop_reason);采样单价按 **$0.0029**(crypt@7680 $0.0037 / @4096 ~$0.002)记账;`--usd-per-step` RL 按 $0.75-2.4 重标;MoE `e_frac_with_tokens`/`e_max_violation` 趋势监控;`tinker.Timeout` 不入 except 元组;reward/advantage 全 numpy(心跳饿死);上传 2MB 块断点续传;save_state ttl 7d;一切训练 reward 用本地严格版,**猎物圈定与记账用官方语义**。

---

## 3. 预算分层与 P(真分 ≥0.865)

| 档 | 内容 | 预期实花 | 硬上限 | **P(≥0.865)·诚实值** |
|---|---|---|---|---|
| **$0 免费** | 融合 3-6 票 + C1 审计 + 全部代码/dry-run | $0-5 | $5 | **~0.10**(T-A 右尾,04 自报且红队认证无乐观偏置) |
| **$40 保守** | + M0($3-5)+ R1 RFT(~$25)+ 探针/杂 | $30-35 | $40 | **~0.15**(RFT 右尾与融合部分重叠) |
| **$80 标准** | + R2a($6-9)或 R2b(~$10)+ D3 consolidation($15)+ 二轮评测 | $55-70 | $80 | **~0.18**(中心仍是显示 0.86=无效奖;买的是右尾+情报) |
| **$150 激进** | + R3 GRPO(≤1000×K8×2pass,$40-60 采样 + 训练/探针,合 ≤$100) | $110-140 | $150 | **~0.20-0.22**(R3 边际只在"覆盖高翻转低"世界里存在) |

校准声明(不许引用为动员数):上述 P 已含世界 B 0.7 折扣与 run-012 高估前科 shading;**任何档位下"多半拿不到 0.865"是基准预期**。对比账:0.86 大包 1495 队、0.87 仅 30 队——0.87 显示分即进真实差异区,这张右尾彩票的赔率(~$80 博 ~18%)由用户定价。
兜底:**最坏情况 = 白扔过期配额 + 触发止损的已花预算;0.86 地板票与 run-011 0.85 在一切分支下物理保全。**

---

## 4. 今晚立即可做清单($0,全部不碰 tinker/modal/kaggle 提交)

1. **资产抢救**(最高优先,重启即灭):
   `mkdir -p data/adapters data/probes && cp -r /tmp/sub_run-011 /tmp/sub_run-012 /tmp/sub_run-011-ep0 data/adapters/`;
   `cp /tmp/{mine_results.jsonl,mine.py,mine.log,rescue_test.py,rescue_test.log,probe80*.py,probe80*.log,probe80_results.json,probe_ft.py,probe_ft.log,crypt_traces.jsonl,oracle_fast.py,oracle_fast_fails.json,oracle_crypt.py,crypt_holdout_fails.json,bit_headroom.py,bit_headroom.json,bit_details.json,oracle_vs_model.json,decode012.py} data/probes/`
2. **`rl/grader_audit.py`(C1,改写最大单块分母)**:用 `external/nemotron-huikang/notebook_tinker.py:434-503` 的 fallback 链重打 `data/run012_holdout_decode.jsonl` 全 770 行;钉死二进制守卫(对照 huikang `reasoning.py:91` docstring 证据);输出修正后猎物名单 `data/prey_holdout.json`(bit/crypt/eq 三层 id+truth)与 `data/prey_train_candidates.json`。
3. **kienngx/huikang 包下载验证**(kaggle CLI 只读,免费):`leegongman/0-86-adapter` + `andreyyunoshev/huikang-nemotron-adapter-mirror`;核 r/alpha/键名/dtype/字节(bf16 1.38GB vs 疑似 fp32 3.27GB);结论写入 `rl/PLAN.md` 附注。**不一致即 T-A 废票/降级重登记。**
4. **`rl/merge_lora.py`**(单版本):QR-SVD 低秩合并 + DARE/TIES 稠密路径 + 五重诊断闸;构建 T-C/T-A1/T-A2 三个包(打包复用 `/tmp/mk_session11.py` 链,先 cp 进 `rl/`)。
5. **`rl/rft_harvest.py` + `rl/rft_filter.py`**:r_harness K>1/T>0 变体 + 落盘 schema(tokens/logprobs/reward);过滤含 C5 选样硬化、crypt cap4096、≤2 条/题、复读检测。
6. **`src/train_tinker.py` 原地加 `--init-from-state`(weights-only fresh Adam)与 `--constant-lr`**(现 resume 在 :262 用 `_with_optimizer`,换调用)。
7. **`rl/anchor_probe.py`**:探针 ≥150 题构成写死(四健康类各 ~30 + eq 已对 40 + bit 已对 40),输出对错 + 平均生成长度漂移 + 重复 n-gram 率;熔断阈值净回撤 ≥3%。
8. **`rl/grpo_loop.py`**(R3 备战):probe→pass→save 顺序写死、KL 子采样默认开、crypt cap4096、零方差丢弃、白名单键剥离;**mock dry-run**:回放 `data/probes/mine_results.jsonl` 走通 datum 构造/journal/熔断/回滚全路径。
9. **账本落地**:PATHS.md 增补——0.86 地板 + 终选条款;新死刑 2 条(alpha/范数缩放 ≥+12.5%、同轨迹 SWA);"营救臂 17%"加注"已训题口径,holdout 未测"。BRIEF 现状账同步。
10. **`rl/QUOTA.md`** 配额台账初始化(§2.2 表)。
11. **比赛规则原创性条款检索**(联网允许):他人公开 adapter 衍生物作终选的合规结论,预登记两套终选名单。
12. (若时间余)`rl/pair_builder.py` DPO 附属臂构对脚本骨架(公共前缀置零 + crypt 截 3072 + 分叉点后负 weight 选项)。

---

## 5. 需要用户拍板的事项

| # | 事项 | 时限 | 默认建议 |
|---|---|---|---|
| 1 | **解除"只设计不花钱"冻结 + 预算档位授权**($40 / $80 / $150) | D1 晨前 | 红队口径:**保守档 $40 发车(02 主攻)**,G2 过线后按需升标准档;激进档可作预授权("G2 判据成立即自动解锁")免临场等待 |
| 2 | **今晚融合 3 票发车确认**(T-C/T-A1/T-A2;现金 $0,用今天过期配额;上传 1.5-4.5h) | **今晚尽早**(配额今夜过期) | 发。kienngx 验证不过则只发 T-C |
| 3 | **终选含他人 adapter 衍生(0.86 票)的取舍** | 规则审计结论后(今晚) | 若规则无碍 → 终选含 0.86 地板票;有碍 → 回退 {run-011, 挑战者} 并接受地板回到 0.85 |
| 4 | R3 GRPO 预授权与否(激进档专属) | 可与 #1 一并 | 仅在"覆盖高+翻转低+LB≥0.86"三条件同时成立时有边际价值,建议预授权但条件写死 |
| 5 | state_ep1 缺失时的 $10 确定性重放 epoch1 | D1 晨(若触发) | 授权(列支在保守档内) |

---

## 6. 预登记预测汇总(写死存档,06-15 复盘对表)

| 量 | 预登记中心(区间) |
|---|---|
| T-C / T-A1 / T-A2 LB | 0.84(0.78-0.85)/ 0.86(P(0.87)≈10%)/ 0.85-0.86(P(0.87)≈6%) |
| M0 覆盖(K8) | bit 35%(15-55)/ crypt 12%(5-30)/ eq 签名:无预测,直测定 |
| R1 holdout 猎物净翻转 | +6(+2~+12) |
| rft_r1 LB | 0.850-0.853(显示大概率 0.85;≥0.86 才算实质信号) |
| 全 campaign 最终真分 | 中心 0.855-0.860(显示 0.86,= 地板,无效奖);P(真分 ≥0.865)按档位 0.10/0.15/0.18/0.22 |
| 复盘条款 | 任何实际值落在区间外 → 写入 EXPERIMENTS.md 校准记录;高估再现则下一战役预测系数再 ×0.8 |
