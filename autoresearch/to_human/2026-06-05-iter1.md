# Autoresearch iter 1 — H1 done, H3 built

## H1 (crypt generator) — CONCLUDED: crypt cannot be SOLVED
Z3 over the full model space: with answers, non-injective fits 100% (degenerate/loose),
injective ~2%. Predict-from-examples (real metric) = ~13%. Ciphers are non-injective +
underdetermined. **Crypt solving is hard-capped ~13-16%. This is the universal wall (the
public field is stuck at 0.85-0.86 for the same reason).**

## H3 (synthetic cipher-deduction corpus) — BUILT (free), staged for retrain
New augmenter src/augmenters/cryptarithm_synth.py: 300 known-answer synthetic cryptarithm
problems (base 10/11, add/sub/mul, injective cipher, 6 examples so the map is deducible),
each with a worked deduction CoT ending in a brace-free \boxed{}. Correct-by-construction,
validated. Registered in AUGMENTER_ALLOWLIST. **Bet: teach the model the cipher-deduction
META-skill so it generalizes where the brittle solver (13%) cannot.** Unmeasurable offline
— needs one paid retrain to validate.

## Honest status
Offline oracle maxed 89.0%. The 0.84->~0.86 gain is mostly the already-banked oracle
improvement (run-005 trained on 87.7%); a retrain realizes it. 0.87+ requires H3 to
actually lift crypt at greedy — uncertain. 0.89 needs crypt ~40% which H1 shows is out of
reach by solving. Next free work: H2 brace-cap workaround, H4 recipe deltas analysis.

## Decision needed from you
One paid 2-epoch retrain (~$20) on the H3-augmented 89.0% corpus would: (a) bank the
oracle gain -> ~0.85 floor, (b) test the H3 crypt meta-skill bet -> possible 0.86-0.87.
You have ~$10; needs a ~$10-15 top-up. I will NOT spend without your go.
