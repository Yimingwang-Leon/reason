# Local dev (Mac arm64, no CUDA). Training/serving happens on remote GPU envs.
# CUDA-only packages (vllm, flash-attn, bitsandbytes, unsloth, triton) are
# pinned in remote envs separately.

# data + analysis
pandas>=2.2
numpy>=1.26
sympy>=1.13

# HF training stack (works on CPU/MPS; we author scripts locally, run remote)
torch>=2.4
transformers>=5.3      # KV-cache bug is fixed in 5.3+; do NOT use trust_remote_code
peft>=0.13             # LoRA config + saving
datasets>=3.0
accelerate>=1.0
trl>=0.12              # SFT / GRPO trainers
safetensors>=0.4
huggingface_hub>=0.26

# kaggle CLI (already installed via conda, listed for reproducibility)
kaggle>=1.6

# Tinker SDK — pin 0.22.3. 0.16.1 is now server-deprecated (create_session 400s).
# The old 0.22.0 download regression (get_checkpoint_archive_url 60s timeout) is
# FIXED in 0.22.3 (verified: run-004 downloaded a 1.4GB checkpoint cleanly).
tinker==0.22.3

# notebook / inspection
jupyter>=1.1
ipykernel>=6.29
matplotlib>=3.9
